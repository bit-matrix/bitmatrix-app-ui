import React from 'react';
import { Asset } from '../../model/Asset';

type Props = {
  assetList: Asset[];
};

export const AssetListModal: React.FC<Props> = () => {
  return <span>AssetList</span>;
};
